{varType FKSDB\Models\Transitions\Holder\ParticipantHolder $holder}
{varType string[]|string[][] $linkArgs}
<p>
    Changes are possible until {$holder->getModel()->event->registration_end|date:'M d, Y; H:i, P'} after
    <a n:href="//:Core:Authentication:login">logging in</a> in <a n:href="//:Core:MyApplications:default">My
        Applications</a> or via this private link:<br/>
    {var $link = $control->link(...$linkArgs)}
    <a href="{$link}">{$link}</a>.
</p>
