{varType FKSDB\Models\Events\Model\Holder\BaseHolder $holder}
<p>
    Změna stavu přihlášky osoby {$holder->getPerson()->getFullName()} na akci <em>participant</em>.
    Nový stav <em>{$state = $holder->getModelState()->label()}</em>.
</p>
{include 'inc/eventEdit.cs.latte'}
{include 'inc/closing.cs.latte'}
