{var $state = $machine->getHolder()->getPrimaryHolder()->getModelState()}
<p>
    {$person->getFullname()}'s application for <em>{$event}</em> state updated.
    New state <em>{$machine->getPrimaryMachine()->getStateName($state)}</em>.
</p>
{include 'inc/eventEdit.en.latte' event => $event, token => $token}
{include 'inc/closing.en.latte'}
