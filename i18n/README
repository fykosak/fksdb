Workflow
--------

1) Write source code with _, gettext and other recognized functions.
2) Run extract.sh, it will create/update messages.po files per each language.
3) Translate messages.po files (e.g. with poedit)
4) Run compile-js.py to build i18n-data.ts file
5) Run compile.sh to build messages.mo files
6) Goto 1)

Versioning
----------

Only messages.po files are kept in the repository.
