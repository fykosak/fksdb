{define seats FKSDB\Components\PDFGenerators\TeamSeating\Place[] $places}
    {foreach $places as $place}
        {var $cor = 'translate(' . $place->x() . ',' . $place->y() . ')'}
        <g transform="{$cor}">
            {include seat 'seat'=>$place}
        </g>
    {/foreach}
{/define}
