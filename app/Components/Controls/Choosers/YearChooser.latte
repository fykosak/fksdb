<ul class="navbar-nav nav">
    <li class="dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
            {$currentYear?($currentYear.'. '._('Ročník')):_('Ročník') }
        </a>
        <ul class="dropdown-menu">
            {foreach $years as $index => $year}
                <li><a class="dropdown-item" href="{link change! $year}">{$year}. Ročník</a></li>
            {/foreach}
        </ul>
    </li>
</ul>
