{varType Fykosak\NetteORM\Model $model}
{varType int $userPermission}
{varType string[] $fields}

<table class="table table-hover">
    <tbody>
    {foreach $fields as $field}
        <tr>
            <th>{control printer:templateString '@' . $field . ':title', $model,$userPermission}</th>
            <td>{control printer:templateString '@' . $field . ':value', $model,$userPermission}</td>
        </tr>
    {/foreach}
    </tbody>
</table>
