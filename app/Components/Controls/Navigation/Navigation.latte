{define #link}
    <a href="{$control->getLink($node)}"
       class="mb-3 d-block navigation-item p-2">
        <span class="icon d-sm-inline-block d-md-block mx-2"><i class="{$control->getTitle($node)->icon}"></i></span>
        <span class="text d-xs-inline-block d-md-block">{$control->getTitle($node)->title}</span>
    </a>
{/define}

{block #menu}
    {var $hasNode = false}
    <nav class="d-md-flex justify-content-md-around flex-wrap">
        {foreach $nodes as $nodeId => $item}
            {var $node => is_array($item) ? $control->getNode($nodeId) : $control->getNode($item)}
            {if $control->isVisible($node)}
                {var $hasNode = true}
                {include #link, node => $node}
            {/if}
        {/foreach}
    </nav>
    {if !$hasNode}
        <p class="alert alert-info">Ups, zdá sa, že tu nič nespravíte :(</p>
    {/if}
{/block}
