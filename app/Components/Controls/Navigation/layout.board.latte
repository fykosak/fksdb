{varType Fykosak\Utils\UI\Navigation\NavItem[] $items}
{varType bool $subTitle}
{block content}
    <nav class="d-md-flex justify-content-md-around flex-wrap">
        {foreach $items as $subItem}
            <a href="{$control->getPresenter()->link($subItem->destination,$subItem->linkParams)}"
               class="mb-3 d-block navigation-item p-2 d-flex align-items-center justify-content-center">
                {$subItem->title->toHtml($subTitle)}
            </a>
        {/foreach}
    </nav>
{/block}
