<div class="mt-3">
    <a class="btn btn-success"
       data-confirm="{sprintf(_('Chcete označiť submit ako správny? úloha: %s, body: %s'),$model->getTask()->label,$model->points)}"
       href="{link check!}">{_'Check'}</a>
</div>
